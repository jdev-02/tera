# Local TAK SDK Tools

Place these local ATAK SDK files here before building:

- `atak-gradle-takdev.jar`
- `main.jar`
- `mapping.txt`
- `android_keystore`

They are intentionally ignored by Git by default.

`USGS_Topo_TERA.xml` is tracked because it is TERA configuration, not an SDK
artifact.
